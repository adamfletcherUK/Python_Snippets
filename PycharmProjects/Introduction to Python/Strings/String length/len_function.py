phrase = """
It is a really long string
triple-quoted strings are used
to define multi-line strings
"""

first_half_len = int(len(phrase) / 2)
print(first_half_len)

first_half = phrase[:first_half_len]
print(first_half)
